[general]
version=hg19
title=Flat File Reference

# type must also include information about whether it is genome vs. position for tsv, vcf, and other easy types.
type=ref
# This datasource does not need a src_file, since the directory is what is needed.  
src_file: