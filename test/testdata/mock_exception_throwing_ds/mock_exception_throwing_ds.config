#This datasource is only meant for testing.  All it ever does is throw exceptions.

[general]
version=v1
title=Mock_Exception

# type must also include information about whether it is genome vs. position for tsv, vcf, and other easy types.
type=mock_exception

# src file is not used for this format.
src_file: not_used