These scripts are not meant for end users.  These are meant for developers/datasource maintainers to generate uniprot datasources (3) and
require a GAF (or other transcript datasource -- such as GENCODE).

These scripts are in need of major refactoring.